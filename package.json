{
  "name": "wcm-graph",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "lint": "tslint --project .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ramda": "^0.23.0"
  },
  "devDependencies": {
    "@types/ramda": "0.0.3",
    "tslint": "^4.4.2",
    "typescript": "^2.1.5"
  }
}
